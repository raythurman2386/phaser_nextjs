export enum EVENTS_NAME {
    chestLoot = "chest-loot",
    attack = "attack",
    gameEnd = "game-over",
}

export enum GAME_STATUS {
    WIN,
    LOSE,
}

export enum SCORE_OPERATIONS {
    INCREASE,
    DECREASE,
    SET_VALUE,
}
